#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main()
{
    // char ch = 'a';
    // char CH = 'A';
    // char news = '\n';
    // char nulls = '\0';

    // cout << (int)ch << endl;
    // cout << (int)CH << endl;
    // cout << (int)news << endl;
    // cout << (int)nulls << endl;

    char str[] = {'a', 'b', 'c', 'd', 'e', 'f'};
    // for (int i = 0; str[i] != '\0'; i++)
    // {
    //     cout << str[i] << " " << endl;
    // }
    cout << str << endl;

    cout << (int)str[6] << endl;
    return 0;
}